#include "main.h"
/**
 * set_plus_flag - Sets the plus_flag in the Flags struct to 1.
 * @flags: A pointer to the Flags struct to modify.
 */
void set_plus_flag(Flags *flags)
{
        flags->plus_flag = 1;
}

/**
 * set_space_flag - Sets the space_flag in the Flags struct to 1.
 * @flags: A pointer to the Flags struct to modify.
 */
void set_space_flag(Flags *flags)
{
        flags->space_flag = 1;
}

/**
 * set_hash_flag - Sets the hash_flag in the Flags struct to 1.
 * @flags: A pointer to the Flags struct to modify.
 */
void set_hash_flag(Flags *flags)
{
        flags->hash_flag = 1;
}

/**
 * set_minus_flag - Sets the minus_flag in the Flags struct to 1.
 * @flags: A pointer to the Flags struct to modify.
 */
void set_minus_flag(Flags *flags)
{
        flags->minus_flag = 1;
}
